import React from 'react';
import { getInitialData } from '../utils/api';

function App() {
  console.log(getInitialData);
  return (
    <div>
      Starter code.
    </div>
  );
}

export default App;
